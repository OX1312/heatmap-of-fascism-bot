# logs / runtime
*.log
bot.launchd.log
bot.log
bot_run.log

# accidental file
PY

# python
__pycache__/
*.pyc
.DS_Store

# secrets
secrets.json

# local helpers / local state
automode
testmode
cache_geocode.json

# runtime state
event_state.json
bot.log
bot.launchd.log
*_run.log

# virtual env
.venv/

# runtime cache
cache_geocode.json

# local log archives
*.bak

# runtime caches (do not commit)
cache_geocode.json
event_state.json

# runtime state / folders (do not commit)
logs/
errors/
secrets/
support/
_backup/
timeline_state.json
pending.json
.autopush_state.json

# tracked cleanup (keep repo readable)
bot.py.bak
trusted_accounts.json

# local backups
*.bak-*

# local backups
reports.geojson.backup
reports.geojson.badjson.backup

# manager notify state
.manager_daily_state.json
.manager_last_update.txt
