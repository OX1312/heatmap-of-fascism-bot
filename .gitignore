# --- Python / OS ---
__pycache__/
*.pyc
.DS_Store
.venv/

# --- Secrets (must never be tracked) ---
secrets/
secrets.json

# --- Runtime state (must never be tracked) ---
state/
pending.json
timeline_state.json
cache_geocode.json
.autopush_state.json
.manager_*.json
.manager_*.txt

# --- Logs (must never be tracked) ---
logs/
*.log

# --- Support / Errors / Backups / Scratch ---
support/
errors/
_backup/
trash/

# --- Local artifacts / misc ---
de.ox.heatmap-bot
tail
*.bak*
*.backup
reports.geojson.*backup
